{
  "name": "mac-block-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "start": "npx lerna run start",
    "start:agent": "npx lerna run start --scope=agent",
    "start:api": "npx lerna run start --scope=api"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
